version: '3.8'
services:
  redis:
    image: redis:7
    restart: always
    ports:
      - "6379:6379"
  backend:
    build: ./backend
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    ports:
      - "5000:5000"